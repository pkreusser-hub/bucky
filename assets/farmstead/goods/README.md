# Farmstead — goods impostor sheet

Generated by `node tools/_fs_bake_goods.cjs`. Do not hand-edit.

| | |
|---|---|
| sheet | `goods.png` 512x1664, 121.9 KB |
| grid | one ROW per good, one COLUMN per azimuth, 8 azimuths, 64 px cells |
| pitch | 52° (the cast sheets' pitch) |
| lighting | camera-relative, key [0.55,1,0.35] |
| frustum | cx 0 cy 0.0625 halfSpan 0.2755, 116.1532 px per camera unit |
| ground anchor | `footPx` (32, 39.26) — the model origin projected once, shared by every cell |

## Reading a cell

    col = round( (objectYaw - cameraYaw) / (2π) * 8 ) mod 8
    row = manifest.rows[<res>].row
    uv  = ( col*cell, row*cell ) .. ( (col+1)*cell, (row+1)*cell )

`texture.flipY = false` — this sheet is written top-left origin. The quad is
VIEW-ALIGNED (the camera's image plane), not world-vertical, because the bake is
an ortho render from a pitched camera; it also needs the same small depth bias
toward the camera the cast layer uses, or the terrain z-tests the near foot away.

The sheet is a DRAW-CALL optimisation, not a fidelity one: `FSModels.goodGeo(res)`
builds the identical mesh and is the automatic fallback when the sheet does not
arrive.

## Goods

- `plank` row 0, silhouette fills 0.61 of its cell
- `stone` row 1, silhouette fills 0.66 of its cell
- `lumber` row 2, silhouette fills 0.62 of its cell
- `boat` row 3, silhouette fills 0.91 of its cell
- `sword` row 4, silhouette fills 0.69 of its cell
- `shield` row 5, silhouette fills 0.51 of its cell
- `goldBar` row 6, silhouette fills 0.51 of its cell
- `goldOre` row 7, silhouette fills 0.62 of its cell
- `steel` row 8, silhouette fills 0.58 of its cell
- `ironOre` row 9, silhouette fills 0.58 of its cell
- `coal` row 10, silhouette fills 0.62 of its cell
- `fish` row 11, silhouette fills 0.87 of its cell
- `bread` row 12, silhouette fills 0.62 of its cell
- `meat` row 13, silhouette fills 0.81 of its cell
- `pig` row 14, silhouette fills 0.74 of its cell
- `wheat` row 15, silhouette fills 0.57 of its cell
- `flour` row 16, silhouette fills 0.51 of its cell
- `shovel` row 17, silhouette fills 0.8 of its cell
- `hammer` row 18, silhouette fills 0.76 of its cell
- `rod` row 19, silhouette fills 0.61 of its cell
- `cleaver` row 20, silhouette fills 0.78 of its cell
- `scythe` row 21, silhouette fills 0.79 of its cell
- `axe` row 22, silhouette fills 0.8 of its cell
- `saw` row 23, silhouette fills 0.67 of its cell
- `pick` row 24, silhouette fills 0.68 of its cell
- `pincer` row 25, silhouette fills 0.55 of its cell
